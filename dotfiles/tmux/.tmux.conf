# this file will be linked to ~/.tmux.conf

# Remap prefix with C-j
unbind C-b
set-option -g prefix C-j
bind C-j send-prefix

# Enable mouse control (clickable windows, panes, resizable panes)
set -g mouse on
